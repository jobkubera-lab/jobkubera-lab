diff --git a/lib/govuk_tech_docs/contribution_banner.rb b/lib/govuk_tech_docs/contribution_banner.rb
--- a/lib/govuk_tech_docs/contribution_banner.rb
+++ b/lib/govuk_tech_docs/contribution_banner.rb
@@
     def report_issue_url
-      url = config[:source_urls]&.[](:report_issue_url)
+      url = config[:tech_docs][:source_urls]&.[](:report_issue_url) ||
+        config[:source_urls]&.[](:report_issue_url)
       params = {
         body: "Problem with '#{current_page.data.title}' (#{config[:tech_docs][:host]}#{current_page.url})",
       }
diff --git a/spec/govuk_tech_docs/source_urls_spec.rb b/spec/govuk_tech_docs/source_urls_spec.rb
--- a/spec/govuk_tech_docs/source_urls_spec.rb
+++ b/spec/govuk_tech_docs/source_urls_spec.rb
@@
-    it "uses a custom URL when configured" do
+    it "uses a custom URL from tech-docs.yaml configuration" do
       current_page = generate_current_page("title", "url")
       config = generate_config("test/repo", "https://test-docs/")
-      config[:source_urls] = {
+      config[:tech_docs][:source_urls] = {
         report_issue_url: "mailto:test@example.com",
       }
       source_urls = GovukTechDocs::SourceUrls.new(current_page, config)
 
       expect(source_urls.report_issue_url).to eql("mailto:test@example.com?body=Problem+with+%27title%27+%28https%3A%2F%2Ftest-docs%2Furl%29&subject=Re%3A+%27title%27")
     end
+
+    it "retains the legacy top-level source_urls fallback" do
+      current_page = generate_current_page("title", "url")
+      config = generate_config("test/repo", "https://test-docs/")
+      config[:source_urls] = {
+        report_issue_url: "mailto:legacy@example.com",
+      }
+      source_urls = GovukTechDocs::SourceUrls.new(current_page, config)
+
+      expect(source_urls.report_issue_url).to eql("mailto:legacy@example.com?body=Problem+with+%27title%27+%28https%3A%2F%2Ftest-docs%2Furl%29&subject=Re%3A+%27title%27")
+    end
+
+    it "prefers the documented tech_docs value over the legacy fallback" do
+      current_page = generate_current_page("title", "url")
+      config = generate_config("test/repo", "https://test-docs/")
+      config[:tech_docs][:source_urls] = {
+        report_issue_url: "mailto:documented@example.com",
+      }
+      config[:source_urls] = {
+        report_issue_url: "mailto:legacy@example.com",
+      }
+      source_urls = GovukTechDocs::SourceUrls.new(current_page, config)
+
+      expect(source_urls.report_issue_url).to start_with("mailto:documented@example.com?")
+    end
