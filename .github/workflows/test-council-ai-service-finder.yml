name: Test Council AI Service Finder

on:
  pull_request:
    paths:
      - "research/council-ai-service-finder/**"
      - ".github/workflows/test-council-ai-service-finder.yml"
  push:
    branches: [main]
    paths:
      - "research/council-ai-service-finder/**"
      - ".github/workflows/test-council-ai-service-finder.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    defaults:
      run:
        working-directory: research/council-ai-service-finder/eval/v0.1
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Run unit tests
        run: python -m unittest discover -v
