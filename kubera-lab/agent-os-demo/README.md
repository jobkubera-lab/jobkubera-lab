# KUBERA Agent OS demo

Assembles work already in this GitHub. Does not call the network on Approve.

Open `index.html` in a browser (same folder as `jobs.json`).

Tabs:

- Lookup → Civic Evidence OS
- Place / events → Community Compass + maps
- Work / visa → prompts and templates
- Control → Agent Fabric approve gate

Do not treat this page as a live council or mail client.
